# Report

## Summary

Markdown template validation completed successfully. The template was validated using model `opencode/deepseek-v4-flash-free` in run `validation-2`. The generated report follows the expected structure with Summary and Details sections.

## Details

- **Test**: Markdown Template Validation
- **Model**: opencode/deepseek-v4-flash-free
- **Run ID**: validation-2
- **Template**: The expected markdown structure requires a `# Report` heading, followed by `## Summary` and `## Details` subheadings, each with placeholder content replaced by actual output.
- **Result**: All required sections are present and properly formatted. The template has been filled with validation-specific content.
